# These run inside a Linux container. A CRLF line ending on the shebang line makes the
# kernel report "bad interpreter", so force LF regardless of the checkout platform.
*.sh    text eol=lf
*.py    text eol=lf
Dockerfile text eol=lf
*.yml   text eol=lf
*.json  text eol=lf

* text=auto
